import { clients } from '../constants';
import styles from '../style'

const Clients = () => (
  <section className={`${styles.flexCenter} my-`}>
    <div></div>
  </section>
)

export default Clients;